# Python program for a calculator
# The purpose of the program is to create a program which allows for addition, subtraction, multiplication and division
# Def is used to create a function 

# This function adds two numbers 
def add(a, b):
    return a + b

# This function subtracts two numbers 
def subtract(a, b):
    return a - b

# This function multiplies two numbers
def multiply(a, b):
    return a * b

# This function divides two numbers
def divide(a, b):
    return a / b

# I decided to call the calculator Modus Operandi and will be giving instructions to the user
print("Modus Operandi")
print("Select the operation you require.")
print("1. Add: +")
print("2. Subtract: -")
print("3. Multiply: *")
print("4. Divide: /")

# You want input from the user and set variables
# input asks user to enter data 
choice = input("Enter choice(1/2/3/4): ")

# num1, num2, choice are variables
# float function allows user to enter numbers (whole numbers or decimals)
num1 = float(input("Enter first number: "))
num2 = float(input("Enter second number: "))

# Will be using if, elif, else statements to set conditions for calculator to operate
# == is used to say equals to
# Do not forget brackets and colons - SYNTAX MATTERS
# Pretty much self-explanatory
if choice == '1':
   print(num1,"+",num2,"=", add(num1,num2))
elif choice == '2':
   print(num1,"-",num2,"=", subtract(num1,num2))
elif choice == '3':
   print(num1,"*",num2,"=", multiply(num1,num2))
elif choice == '4':
   print(num1,"/",num2,"=", divide(num1,num2))
else:
     print("ERROR:PLEASE TRY AGAIN")
